<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lake Elementary Pokemon Club Website" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/pokemon-club-favicon.png" type="image/png">
    <link rel="apple-touch-icon" href="/pokemon-club-favicon.png">
    <link rel="shortcut icon" href="/pokemon-club-favicon.png" type="image/png">

    <%# Bootstrap 5 CSS from CDN %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <%# Font Awesome Icons %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <%# FullCalendar - only load on events pages %>
    <% if controller_name == 'events' %>
      <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.js"></script>
    <% end %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <!-- Brand -->
        <%= link_to "Pokemon Club", root_path, class: "navbar-brand fw-bold" %>

        <!-- Mobile toggle button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation items -->
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Events", events_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Students", students_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Donors", public_donors_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Resources", public_resources_path, class: "nav-link" %>
            </li>
          </ul>

          <!-- Authentication links -->
          <ul class="navbar-nav">
            <% if authenticated? %>
              <!-- User is logged in -->
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class="fas fa-user-circle me-2"></i>
                  <%= current_user&.first_name || "User" %>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Profile", user_path, class: "dropdown-item" %></li>
                  <% if current_user.respond_to?(:admin?) && current_user.admin? %>
                    <li><hr class="dropdown-divider"></li>
                    <li><%= link_to "User Management", admin_users_path, class: "dropdown-item" %></li>
                    <li><%= link_to "Donor Management", admin_donors_path, class: "dropdown-item" %></li>
                    <li><%= link_to "Document Management", admin_documents_path, class: "dropdown-item" %></li>
                    <li><hr class="dropdown-divider"></li>
                  <% end %>
                  <li>
                    <%= link_to "Logout", session_path, data: { "turbo-method": "delete" }, class: "dropdown-item" %>
                  </li>
                </ul>
              </li>
            <% else %>
              <!-- User is not logged in -->
              <li class="nav-item">
                <%= link_to "Login", new_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <% if notice || alert %>
      <div class="container mt-3">
        <% if notice %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        <% if alert %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Loading Spinner -->
    <div id="loading-spinner" class="d-none position-fixed top-0 start-0 w-100 h-100" style="z-index: 9999; background-color: rgba(255, 255, 255, 0.9);">
      <div class="d-flex align-items-center justify-content-center h-100">
        <div class="text-center">
          <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
          <div class="mt-3">
            <h5 class="text-primary">Loading...</h5>
            <p class="text-muted small">Please wait while we fetch your content</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Main content -->
    <main class="container mt-4">
      <%= yield %>
    </main>

    <script>
      // Global loading spinner functionality
      document.addEventListener('DOMContentLoaded', function() {
        const spinner = document.getElementById('loading-spinner');

        // Show spinner on Turbo navigation
        document.addEventListener('turbo:visit', function() {
          if (spinner) {
            spinner.classList.remove('d-none');
          }
        });

        // Hide spinner when page loads
        document.addEventListener('turbo:load', function() {
          if (spinner) {
            spinner.classList.add('d-none');
          }
        });

        // Show spinner on form submissions
        document.addEventListener('turbo:submit-start', function() {
          if (spinner) {
            spinner.classList.remove('d-none');
          }
        });

        // Hide spinner after form submission
        document.addEventListener('turbo:submit-end', function() {
          if (spinner) {
            spinner.classList.add('d-none');
          }
        });

        // Show spinner for AJAX requests
        document.addEventListener('ajax:before', function() {
          if (spinner) {
            spinner.classList.remove('d-none');
          }
        });

        document.addEventListener('ajax:complete', function() {
          if (spinner) {
            spinner.classList.add('d-none');
          }
        });

        // Fallback: Hide spinner after a maximum time to prevent it from getting stuck
        let spinnerTimeout;
        document.addEventListener('turbo:visit', function() {
          clearTimeout(spinnerTimeout);
          spinnerTimeout = setTimeout(function() {
            if (spinner) {
              spinner.classList.add('d-none');
            }
          }, 10000); // Hide after 10 seconds maximum
        });

        document.addEventListener('turbo:load', function() {
          clearTimeout(spinnerTimeout);
        });
      });

      // Global function to show/hide spinner programmatically
      window.showLoadingSpinner = function() {
        const spinner = document.getElementById('loading-spinner');
        if (spinner) {
          spinner.classList.remove('d-none');
        }
      };

      window.hideLoadingSpinner = function() {
        const spinner = document.getElementById('loading-spinner');
        if (spinner) {
          spinner.classList.add('d-none');
        }
      };
    </script>
  </body>
</html>
